name: chapter_reader
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Firebase packages for Realtime Sync
  firebase_core: ^3.10.0
  firebase_database: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  peanut: ^6.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/chapters/